import React from 'react';
import PropTypes from 'prop-types';

const Result = ({ props }) => {

  return (
    <div>
      Ima result!
    </div>
  );
};

Result.defaultProps = {
  
};

Result.propTypes = {
  
};

export default Result;
